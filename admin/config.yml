backend:
    name: github
    repo: rainshadedesign/jekyll-netlify-test

media_folder: "assets/uploads"

collections:
  - name: "Navigation"
    label: "Navigation"
    files:
      - label: "Navigation"
        name: "navigation"
        file: "_data/navigation.yml"
        fields:
          - label: "Name"
            name: "name"
            width: "string"
